# Canonical public URL of the marketing site (used for SEO metadata, sitemap, OG)
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# Where the "Start free" / app CTAs point (the web client)
NEXT_PUBLIC_APP_URL=http://localhost:3000
