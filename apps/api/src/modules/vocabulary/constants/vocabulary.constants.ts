/** Max due review cards pulled into a single study session. */
export const REVIEW_SESSION_LIMIT = 20;

/** Max brand-new cards (no progress yet) introduced per study session. */
export const NEW_CARDS_PER_SESSION = 10;
